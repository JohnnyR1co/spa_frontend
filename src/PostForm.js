import React from 'react';
import ReactDOM from 'react-dom';

export default class PostForm extends React.Component {
  render() {
    return (
      <div>
        Добавление записи запрещено
      </div>
    );
  }
}
